DEBUG=True
SECRET_KEY=dev-secret-key-change-in-production
ALLOWED_HOSTS=localhost,127.0.0.1

POSTGRES_DB=jobboard
POSTGRES_USER=jobboard
POSTGRES_PASSWORD=jobboard
POSTGRES_HOST=localhost
POSTGRES_PORT=5433

CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

CELERY_BROKER_URL=redis://localhost:6379/0

# django.core.mail.backends.smtp.EmailBackend for real delivery, or
# django.core.mail.backends.console.EmailBackend to just print emails locally.
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=Noticeboard <noreply@noticeboard.local>

FRONTEND_URL=http://localhost:3000
