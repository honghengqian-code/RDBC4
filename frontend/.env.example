NEXT_PUBLIC_API_URL=http://localhost:8000/api

# Server-side (SSR) fetches only — set in docker-compose.yml to hit the
# backend container directly (http://backend:8000/api). Leave unset for
# plain `npm run dev`, where it falls back to NEXT_PUBLIC_API_URL above.
# API_URL=
