import EmployerLoginForm from "@/components/EmployerLoginForm";

export const metadata = { title: "Employer sign in — Noticeboard" };

export default function EmployerLoginPage() {
  return <EmployerLoginForm />;
}
